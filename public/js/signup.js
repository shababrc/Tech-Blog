const signupFormHandler = async () => {

//capture the values from username and password

//send the data over to the correct routes

//if response ok, redirect to dashboard (need to make),
//for now redirect to homepage

}
//create event listener on the button to click 
 